<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/dashboard/logoponal.png" type="image/png">
    <title>CA Colombia | Base de Datos Policial</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/alpinejs" defer></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    
    <style>
        :root {
            --police-dark: #004830;
            --police-medium: #005d3e;
            --police-light: #006b47;
            --accent-gold: #d4af37;
            --accent-gold-light: #e8c547;
            --text-dark: #1a1a1a;
            --bg-light: #f5f7fa;
        }
        
        * {
            font-family: 'Roboto', 'Segoe UI', sans-serif;
        }

        body {
            background: linear-gradient(135deg, var(--bg-light) 0%, #ffffff 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            color: var(--text-dark);
        }

        main {
            flex: 1 0 auto;
        }

        footer {
            flex-shrink: 0;
            width: 100%;
            margin-top: auto;
        }

        .header-container {
            background: linear-gradient(to right, var(--police-dark), var(--police-medium), var(--police-light));
            box-shadow: 0 8px 32px rgba(0, 72, 48, 0.3);
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
            z-index: 1000;
            backdrop-filter: blur(10px);
        }

        .login-card {
            background: rgba(255, 255, 255, 0.99);
            box-shadow: 0 20px 60px rgba(0, 72, 48, 0.15);
            border-radius: 2rem;
            border: 1px solid rgba(0, 72, 48, 0.1);
            position: relative;
            overflow: hidden;
        }

        .login-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(to right, var(--police-dark), var(--accent-gold), var(--police-light));
        }

        .login-card::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 100% 0%, rgba(0, 72, 48, 0.05), transparent 50%);
            pointer-events: none;
        }

        .discord-button {
            position: relative;
            border: none;
            background: none;
            cursor: pointer;
            padding: 0;
            outline: none;
        }

        .discord-button:hover {
            transform: translateY(-3px);
        }

        .discord-button svg {
            width: 24px;
            height: 24px;
            flex-shrink: 0;
            fill: currentColor;
            transition: transform 0.3s ease;
        }

        .discord-button:hover svg {
            transform: scale(1.15);
        }

        nav a {
            color: rgba(255, 255, 255, 0.95);
            font-weight: 500;
            padding: 0.75rem 1.25rem;
            border-radius: 0.75rem;
            transition: all 0.3s ease;
            font-size: 0.95rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        nav a:hover {
            color: var(--accent-gold);
            background: rgba(212, 175, 55, 0.15);
            transform: translateY(-2px);
        }

        .mobile-menu {
            position: fixed;
            top: 0;
            right: -100%;
            width: 100%;
            height: 100vh;
            background: linear-gradient(135deg, var(--police-dark), var(--police-medium));
            transition: all 0.3s ease-in-out;
            padding-top: 5rem;
            z-index: 40;
            box-shadow: -8px 0 32px rgba(0, 72, 48, 0.3);
        }

        .mobile-menu.active {
            right: 0;
        }

        .mobile-menu a {
            display: block;
            padding: 1.25rem 2rem;
            color: white;
            font-size: 1rem;
            border-bottom: 1px solid rgba(212, 175, 55, 0.2);
            font-weight: 500;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .mobile-menu a:hover {
            background: rgba(212, 175, 55, 0.15);
            color: var(--accent-gold);
            padding-left: 2.5rem;
        }

        footer {
            background: linear-gradient(135deg, var(--police-dark), #003824);
            border-top: 1px solid rgba(212, 175, 55, 0.2);
        }

        .hamburger {
            display: block;
            width: 24px;
            height: 20px;
            position: relative;
            z-index: 50;
        }

        .hamburger span {
            display: block;
            height: 2.5px;
            background-color: white;
            margin-bottom: 5px;
            transition: all 0.3s ease;
            border-radius: 2px;
        }

        .hamburger.active span:nth-child(1) {
            transform: rotate(45deg) translate(8px, 8px);
        }

        .hamburger.active span:nth-child(2) {
            opacity: 0;
        }

        .hamburger.active span:nth-child(3) {
            transform: rotate(-45deg) translate(7px, -7px);
        }

        .btn-discord {
            background: linear-gradient(135deg, var(--police-dark) 0%, var(--police-medium) 50%, var(--police-light) 100%);
            color: white;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 10px 30px rgba(0, 72, 48, 0.3);
            border: 1px solid rgba(212, 175, 55, 0.3);
            position: relative;
            overflow: hidden;
        }

        .btn-discord::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .btn-discord:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 50px rgba(0, 72, 48, 0.4);
            background: linear-gradient(135deg, var(--police-medium) 0%, var(--police-light) 100%);
        }

        .btn-discord:hover::before {
            left: 100%;
        }

        .section-title {
            color: var(--police-dark);
            font-weight: 700;
            letter-spacing: 0.5px;
        }

        .divider {
            background: linear-gradient(to right, transparent, var(--accent-gold), transparent);
            height: 2px;
        }

        .alert-custom {
            background: linear-gradient(135deg, rgba(220, 38, 38, 0.1), rgba(220, 38, 38, 0.05));
            border: 1px solid rgba(220, 38, 38, 0.3);
            color: #7f1d1d;
        }

        .logo-container img {
            filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
            transition: transform 0.3s ease;
        }

        .logo-container:hover img {
            transform: scale(1.05);
        }

        @media (min-width: 768px) {
            .hamburger {
                display: none;
            }
        }

        @media (max-width: 768px) {
            .discord-button div {
                width: 100%;
                justify-content: center;
                padding: 1rem 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .header-container {
                padding: 0.75rem 1rem;
            }

            h2 {
                font-size: 1.75rem;
            }

            .login-card {
                padding: 1.75rem;
                border-radius: 1.5rem;
            }

            .discord-button div {
                padding: 0.875rem 1.25rem;
                font-size: 0.95rem;
            }
            
            .discord-button svg {
                width: 20px;
                height: 20px;
            }
            
            .discord-button span {
                font-size: 0.9rem;
            }

            nav a {
                padding: 0.5rem 0.75rem;
                font-size: 0.85rem;
            }
        }

        /* Animación de pulsación */
        @keyframes subtle-pulse {
            0%, 100% { box-shadow: 0 10px 30px rgba(0, 72, 48, 0.3); }
            50% { box-shadow: 0 15px 40px rgba(0, 72, 48, 0.4); }
        }

        .btn-discord:hover {
            animation: subtle-pulse 1s ease-in-out;
        }
    </style>
</head>

<body>
    <header class="header-container">
        <div class="max-w-7xl mx-auto px-6 py-4">
            <div class="flex justify-between items-center">
                <div class="logo-container">
                    <img src="/login/logo_ponal.png" alt="Policía Nacional de Colombia" 
                         class="h-16 w-auto">
                </div>
                
                <!-- Navegación desktop -->
                <nav class="hidden md:flex space-x-4">
                    <a href="https://app.cacolombia.com/police-page.html">INICIO</a>
                    <a href="https://discord.gg/cacolombiarp">CONTACTÉNOS</a>
                    <a href="https://app.cacolombia.com/notas.html">NOTAS</a>
                </nav>

                <!-- Botón hamburguesa -->
                <div class="hamburger md:hidden cursor-pointer" id="hamburger">
                    <span class="block w-6 h-0.5 bg-white mb-1.5"></span>
                    <span class="block w-6 h-0.5 bg-white mb-1.5"></span>
                    <span class="block w-6 h-0.5 bg-white"></span>
                </div>
            </div>
        </div>

        <!-- Menú móvil -->
        <div class="mobile-menu md:hidden" id="mobile-menu">
            <nav class="flex flex-col">
                <a href="https://app.cacolombia.com/police-page.html">INICIO</a>
                <a href="https://discord.gg/cacolombiarp">CONTACTÉNOS</a>
                <a href="https://app.cacolombia.com/notas.html">NOTAS</a>
            </nav>
        </div>
    </header>

    <main class="max-w-2xl mx-auto px-6 pt-32 pb-20 w-full">
        <section class="login-card rounded-3xl p-12" data-aos="fade-up">
            <div class="text-center mb-12">
                <h2 class="section-title text-4xl md:text-5xl mb-4">Base de Datos Policial</h2>
                <div class="divider mx-auto w-24 mb-4"></div>
                <p class="text-gray-600 font-light text-base">Sistema Oficial de Consulta</p>
            </div>

            <% if (typeof message !== 'undefined') { %>
                <div class="alert-custom border border-red-400 text-red-700 px-6 py-4 rounded-xl relative mb-8" role="alert">
                    <div class="flex items-center">
                        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4v.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <span class="block sm:inline font-medium"><%= message %></span>
                    </div>
                </div>
            <% } %>

            <div class="text-center space-y-8">
                <button onclick="discordauth()" class="discord-button group w-full">
                    <div class="relative inline-flex items-center gap-4 px-8 py-4 bg-gradient-to-r btn-discord rounded-xl text-[#d4af37] font-semibold shadow-lg transition-all duration-300 w-full justify-center">
                        <svg class="w-6 h-6 transition-transform group-hover:scale-110" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 127.14 96.36" fill="currentColor">
                            <path d="M107.7,8.07A105.15,105.15,0,0,0,81.47,0a72.06,72.06,0,0,0-3.36,6.83A97.68,97.68,0,0,0,49,6.83,72.37,72.37,0,0,0,45.64,0,105.89,105.89,0,0,0,19.39,8.09C2.79,32.65-1.71,56.6.54,80.21h0A105.73,105.73,0,0,0,32.71,96.36,77.7,77.7,0,0,0,39.6,85.25a68.42,68.42,0,0,1-10.85-5.18c.91-.66,1.8-1.34,2.66-2a75.57,75.57,0,0,0,64.32,0c.87.71,1.76,1.39,2.66,2a68.68,68.68,0,0,1-10.87,5.19,77,77,0,0,0,6.89,11.1A105.25,105.25,0,0,0,126.6,80.22h0C129.24,52.84,122.09,29.11,107.7,8.07ZM42.45,65.69C36.18,65.69,31,60,31,53s5-12.74,11.43-12.74S54,46,53.89,53,48.84,65.69,42.45,65.69Zm42.24,0C78.41,65.69,73.25,60,73.25,53s5-12.74,11.44-12.74S96.23,46,96.12,53,91.08,65.69,84.69,65.69Z"/>
                        </svg>
                        <span class="relative">Ingresa con Discord</span>
                    </div>
                </button>

                <div class="bg-blue-50 border border-blue-200 rounded-xl p-6 mt-8">
                    <p class="text-gray-700 text-sm leading-relaxed font-light">
                        <span class="font-semibold text-var(--police-dark)">Requisitos de acceso:</span><br>
                        Se requiere tener un rol de policía, tránsito, fiscalía, CTI o relacionados para poder acceder a la base de datos.
                    </p>
                </div>
            </div>
        </section>
    </main>

    <footer class="text-white mt-12 py-10 px-6">
        <div class="max-w-4xl mx-auto text-center">
            <p class="text-sm leading-relaxed text-gray-300 font-light">
                Este sitio web es un proyecto de ficción y parodia inspirado en actividades de roleplay.<br>
                No está afiliado, patrocinado ni respaldado por la Policía Nacional de Colombia ni por alguna entidad pública o privada.<br>
                Todos los nombres, logotipos y referencias son utilizados con fines creativos.<br>
                <span class="text-accent-gold font-semibold">Página exclusiva de 「CA」 Colombia ER:LC</span>
            </p>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            AOS.init({
                duration: 1000,
                offset: 100,
                once: true,
                disable: 'mobile',
                easing: 'ease-out-cubic'
            });

            // Funcionalidad del menú hamburguesa
            const hamburger = document.getElementById('hamburger');
            const mobileMenu = document.getElementById('mobile-menu');
            const body = document.body;

            hamburger.addEventListener('click', function() {
                this.classList.toggle('active');
                mobileMenu.classList.toggle('active');
                body.classList.toggle('overflow-hidden');
            });

            // Cerrar menú al hacer click en un enlace
            const mobileLinks = mobileMenu.getElementsByTagName('a');
            Array.from(mobileLinks).forEach(link => {
                link.addEventListener('click', () => {
                    hamburger.classList.remove('active');
                    mobileMenu.classList.remove('active');
                    body.classList.remove('overflow-hidden');
                });
            });

            // Cerrar menú al redimensionar la ventana
            window.addEventListener('resize', () => {
                if (window.innerWidth > 768) {
                    hamburger.classList.remove('active');
                    mobileMenu.classList.remove('active');
                    body.classList.remove('overflow-hidden');
                }
            });
        });

        function discordauth() {
            window.open('/v1/auth/discord');
        }
    </script>
</body>
</html>
